from setuptools import setup, find_packages

setup(
    name="cars_detector",
    version="0.0.0",
    description="",
    author="Timothee RIO",
    packages=["cars_detector"],
    package_dir={
        "cars_detector": "cars_detector"}
)